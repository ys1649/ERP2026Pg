# CLAUDE.md — ERP2026Pg 專案指示

這是 Delphi6ERP（Delphi 6 + MSSQL 桌面版 ERP）移植到 ERP2026（React + FastAPI + PostgreSQL）的專案。**這是移植，不是重新設計**——所有交易單據/總帳/報表功能的商業邏輯，都必須跟舊系統的實際行為對齊。

## 開始工作前必讀

1. **[`PROJECT.md`](PROJECT.md)**（本專案根目錄）——新系統的技術文件：目錄結構、資料庫、API、前後端已完成/待開發功能清單。每次開始工作前先讀一次，因為它會持續更新反映最新狀態。
2. **[`Delphi6ERP/ProjectSummary.md`](Delphi6ERP/ProjectSummary.md)**——舊系統的架構/模組/商業邏輯/已知問題總覽。
3. 要實作或修改任何交易單據/總帳/報表功能之前，**先讀舊系統對應模組的原始碼**（`Delphi6ERP/ERP/Program.DLL/<模組資料夾>/*.pas`），不要憑空設計——資料庫裡的真實資料就是照舊系統的資料模型跑出來的。

## 核心原則：完全比照舊系統邏輯，包含已知的缺陷/怪異行為

使用者明確要求：**轉換邏輯要跟舊 Delphi 程式一模一樣**，包含讀碼時發現的不一致、看起來像 bug 的行為、甚至疏漏，都要原樣重現，而不是「順便修正」或「跨模組統一成比較好的做法」。

- 不同模組的確認/取消確認機制本來就不一致（SHIP/PO_RECV/INV_ADJUST 有兩段式 Book/Unbook；AR_RECV/AP_PAY/傳票維護完全沒有，存檔即生效）——照抄各自的行為，不要統一。
- 已發現的具體案例：PO_RECV「僅列印本張」選項是死碼、INV_ADJUST 不檢查虛擬商品、庫存調整完全不過帳到總帳、傳票維護可以任意改掉自動過帳產生的傳票且無防護、`GetSupHisPrice` 查無歷史價格時沒有像 `GetCusHisPrice` 一樣的後備值……這類發現到的行為都是刻意原樣保留的實作結果，不是還沒處理的 TODO。
- 例外：**平台限制**（不是商業邏輯本身）需要用網頁上功能對等的做法取代，例如 Delphi 的 `Application.Terminate` 強制重啟、ReportBuilder 報表的累計小計欄位——這類不需要每次都重新確認，直接找功能對等的 Web 實作即可。
- 真的遇到「即使照抄舊邏輯，兩份舊程式碼互相矛盾」或「完全看不出設計意圖」的情況才需要停下來問清楚（例如現金簿到底是走動態報表引擎還是獨立客製表單、試算表的實際定義是淨額兩欄式還是期初期末四欄式），不要每個發現的小怪癖都各自發問。

## 開發慣例

- 溝通與 commit / PR 一律使用繁體中文。
- 交易單據模組共用兩支後端引擎，之後新模組直接重用、不要重寫：
  - `backend/services/inventory.py`（移動平均成本，逐字比照 `erp_public.pas` 的 `InsertTransaction`/`UpdateTransaction`）
  - `backend/services/accounting.py`（GL 過帳/借貸平衡檢查）
- 每完成一個模組的後端+前端，一定要在瀏覽器裡用真實或可拋棄的測試資料跑過完整生命週期（新增→確認/收付款→取消確認→刪除），並用資料庫查詢確認測試資料完全清乾淨、相關聯的庫存量/成本/未清餘額都精確還原到測試前的基準值。
- 後端/前端 dev server 由使用者自己啟動/關閉，不要主動幫忙重啟或關閉。
